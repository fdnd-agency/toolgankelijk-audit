# The URL of your Directus API endpoint (e.g., https://your-instance.directus.app)
DIRECTUS_URL=""

# The static token for accessing your Directus content (from Directus User Settings -> Token)
DIRECTUS_STATIC_TOKEN=""

# The frontend origin allowed to call the audit backend (CORS configuration)
# Examples:
# CORS_ORIGIN="http://localhost:5173" (local development)
# CORS_ORIGIN="https://toolgankelijk.onrender.com" (production)
CORS_ORIGIN=""
